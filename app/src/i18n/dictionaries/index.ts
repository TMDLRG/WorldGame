import { en } from './en'
import { es } from './es'
import { hi } from './hi'

export { en, es, hi }

export const DICTIONARIES_VERSION = '1.0.0' as const
export const EXPECTED_KEY_COUNT = Object.keys(en).length
